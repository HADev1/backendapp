const User = require("./user");
const Room = require("./room");
const ProviderDetail = require("./provider-detail");
const Message = require("./message");

module.exports = { User, Room, ProviderDetail, Message };
